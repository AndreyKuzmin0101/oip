# Напарник: Кузьмин Андрей

# Задание 5 — Векторный поиск по TF-IDF

## Цель

Разработать поисковую систему на основе векторного поиска по построенному индексу.

---

## Что было сделано

1. Загружены TF-IDF индексы документов (директории tf-idf/tokens/ и tf-idf/lemmas/).

2. Для каждого документа сформирован разрежённый вектор в пространстве терминов.

3. Реализована токенизация и лемматизация запроса. Запрос разбивается на токены, приводится к нормальной форме (если выбран режим lemmas), и очищается от стоп-слов.

4. Определение степени схожести между вектором запроса и каждым документом происходит методом косинусного сходства.

5. Вывод результатов. Документы сортируются по убыванию сходства, и возвращается топ-N результатов с указанием веса и имени файла.

---

## Структура файлов

```text
pages/
├── 1_sample.html
├── 2_sample.html
├── ...
│
tf-idf/
├── tokens/
│   ├── 1-tfidf.txt
│   └── ...
├── lemmas/
│   ├── 1-tfidf.txt
│   └── ...
│
search_system.py
```

## Используемая метрика — косинусное сходство

Косинусное сходство между вектором запроса q и вектором документа d вычисляется по формуле:

$$
\Huge
\text{cosine}(\vec{q}, \vec{d}) = \frac{ \vec{q} \cdot \vec{d} }{ \| \vec{q} \| \cdot \| \vec{d} \| }
$$

- ( $\vec{q} \cdot \vec{d}$ ) — скалярное произведение векторов  
- ( $| \vec{q} |$ ) и ( $| \vec{d} |$ ) — нормы (длины) векторов

Значения сходства лежат в диапазоне от 0 до 1, где 1 означает полное совпадение векторов, а 0 – их полную разность.

---

## Пример работы

Вывод:
```text
> Режим поиска (tokens/lemmas):
```

Ввод:
```text
> tokens
```

Вывод:
```text
> Поисковая система успешно проинициализирована
> Введите поисковый запрос или введите 'q' для выхода:
```

Ввод:
```text
> Андрей
```

Вывод:
```text
Найдено документов: 9
1. Сходство: 0.1257. Документ: 2-elementy.ru.html
2. Сходство: 0.0625. Документ: 11-kot.sh.html
3. Сходство: 0.0533. Документ: 6-scientificrussia.ru.html
4. Сходство: 0.0208. Документ: 17-perpetuum.html
5. Сходство: 0.0204. Документ: 38-e-plus.media.html
6. Сходство: 0.0188. Документ: 43-fractals.html
7. Сходство: 0.0186. Документ: 37-fiop.site.html
8. Сходство: 0.0164. Документ: 36-434015.html
9. Сходство: 0.0116. Документ: 100-dumaikids.ru.html
```

---

## Результат

- Для каждого документа рассчитаны значения TF-IDF по токенам и леммам
- Значения сохранены в читаемом формате: термин IDF TF-IDF
- Данные можно использовать для ранжирования, кластеризации или визуализации